```
./docker-compose-logparser -h
```

```
Docker Log Parser 0.1
Ruslan Tahiiev
Parse docker logs

USAGE:
    docker-compose-logparser [OPTIONS] --name <app>

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

OPTIONS:
    -n, --name <app>                    App name
    -p, --path </path/where/execute>    Path where command must be executed
```